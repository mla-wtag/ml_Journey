<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" type="text/css" href="stylesheets/navbar.css">

</head>
<body>
  <header>
    <nav class='nav-items'>
      <%= link_to user_path(current_user), class: 'nav-link' do %>
      <%= File.read(Rails.root.join('app', 'assets', 'icons', 'profile.svg')).html_safe %>
      <% end %>
      <%= link_to t('views.journal'), user_journals_path(current_user), class: 'nav-link' %> 
      <%= link_to t('views.task'), user_tasks_path(current_user), class: 'nav-link' %>
      <%= link_to t('views.members'), users_path, class: 'nav-link' %>
      <%= link_to t('buttons.log_out'), new_user_session_path, data: { turbo_method: :delete }, class: 'nav-link' %>
    </nav>
  </header>
</body>
</html>
